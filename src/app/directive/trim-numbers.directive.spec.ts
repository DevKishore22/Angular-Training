import { TrimNumbersDirective } from './trim-numbers.directive';

describe('TrimNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
